# Прод собирается под nginx: /api/ и /files/ проксируются с того же origin,
# поэтому база пустая → fetch('/api/calculate'). Не удалять: без этой строки
# config.ts подставит дефолт http://localhost:3000 и фронт пойдёт на localhost клиента.
VITE_API_URL=
